function Parzen1D(dataset, std)
    
    % plot
    x_min = min(dataset);
    x_max = max(dataset);
    x = (x_min - 2):0.01:(x_max + 2);
    y1 = zeros(size(x));
    N = length(dataset);


    for i = 1:size(x,2)
        p = zeros(N); % reset
        for j = 1:N
            p[j] = normpdf(x(i), dataset(j), std);
            




end